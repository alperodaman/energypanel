# @enerjipanel/eslint-config

EnerjiPanel servisleri için paylaşılan ESLint flat config (`neostandard` + Prettier uyumu).

## Kullanım

Bir serviste `eslint.config.js`:

```js
const enerjipanelConfig = require('@enerjipanel/eslint-config');

module.exports = [...enerjipanelConfig];
```
