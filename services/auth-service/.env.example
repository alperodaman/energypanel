PORT=4001

# postgres-auth container'ına localhost'a map'li port üzerinden bağlanır (bkz. root .env.example, POSTGRES_AUTH_PORT)
DATABASE_URL="postgresql://enerjipanel:change-me@localhost:5433/auth_db?schema=public"

JWT_SECRET=change-me-to-a-long-random-string
